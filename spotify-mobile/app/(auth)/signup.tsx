import{useState}from"react";import{View,Text,TextInput,TouchableOpacity,Alert}from"react-native";import{useRouter}from"expo-router";import{useAuthStore}from"@/store/authStore";
export default function Signup(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const signup=useAuthStore(s=>s.signup);const router=useRouter();const submit=async()=>{try{await signup(name.trim(),email.trim(),password);router.replace("/(tabs)")}catch(e:any){Alert.alert("Sign up failed",e?.response?.data?.message||e?.message||"Try again")}};return <View className="flex-1 bg-black justify-center px-6"><Text className="text-spotify text-4xl font-bold text-center">TuneStream</Text><Text className="text-white text-2xl font-bold text-center mt-2 mb-8">Create account</Text><TextInput placeholder="Name" placeholderTextColor="#888" value={name} onChangeText={setName} className="bg-gray-800 text-white p-4 rounded-xl mb-4"/><TextInput placeholder="Email" placeholderTextColor="#888" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" className="bg-gray-800 text-white p-4 rounded-xl mb-4"/><TextInput placeholder="Password" placeholderTextColor="#888" value={password} onChangeText={setPassword} secureTextEntry className="bg-gray-800 text-white p-4 rounded-xl mb-6"/><TouchableOpacity onPress={submit} className="bg-spotify py-4 rounded-full"><Text className="text-black font-bold text-center text-lg">Sign up</Text></TouchableOpacity><TouchableOpacity onPress={()=>router.push("/(auth)/login")} className="mt-6"><Text className="text-gray-400 text-center">Already have an account? <Text className="text-white font-semibold">Log in</Text></Text></TouchableOpacity></View>}