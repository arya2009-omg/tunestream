import{useState}from"react";import{View,Text,TextInput,TouchableOpacity,Alert}from"react-native";import{useRouter}from"expo-router";import{useAuthStore}from"@/store/authStore";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const login=useAuthStore(s=>s.login);const router=useRouter();const submit=async()=>{try{await login(email.trim(),password);router.replace("/(tabs)")}catch(e:any){Alert.alert("Login failed",e?.response?.data?.message||e?.message||"Try again")}};return <View className="flex-1 bg-black justify-center px-6"><Text className="text-spotify text-4xl font-bold text-center">TuneStream</Text><Text className="text-white text-2xl font-bold text-center mt-2 mb-8">Log in</Text><TextInput placeholder="Email" placeholderTextColor="#888" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" className="bg-gray-800 text-white p-4 rounded-xl mb-4"/><TextInput placeholder="Password" placeholderTextColor="#888" value={password} onChangeText={setPassword} secureTextEntry className="bg-gray-800 text-white p-4 rounded-xl mb-6"/><TouchableOpacity onPress={submit} className="bg-spotify py-4 rounded-full"><Text className="text-black font-bold text-center text-lg">Log in</Text></TouchableOpacity><TouchableOpacity onPress={()=>router.push("/(auth)/signup")} className="mt-6"><Text className="text-gray-400 text-center">Don't have an account? <Text className="text-white font-semibold">Sign up</Text></Text></TouchableOpacity></View>}