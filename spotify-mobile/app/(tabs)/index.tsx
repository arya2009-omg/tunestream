import{useEffect,useState}from"react";import{Text,ScrollView,FlatList,RefreshControl}from"react-native";import{SafeAreaView}from"react-native-safe-area-context";import api from"@/lib/api";import SongCard from"@/components/SongCard";import SongRow from"@/components/SongRow";import{Song}from"@/store/playerStore";
export default function Home(){const[songs,setSongs]=useState<Song[]>([]);const[refreshing,setRefreshing]=useState(false);const load=async()=>{setRefreshing(true);try{setSongs((await api.get("/songs")).data)}catch{}finally{setRefreshing(false)}};useEffect(()=>{load()},[]);const h=new Date().getHours();const greeting=h<12?"Good morning":h<18?"Good afternoon":"Good evening";return <SafeAreaView className="flex-1 bg-black"><ScrollView refreshControl={<RefreshControl refreshing={refreshing} onRefresh={load} tintColor="#1DB954" />}><Text className="text-white text-3xl font-bold px-4 pt-5">{greeting} 🎧</Text><Text className="text-gray-400 px-4 mt-1">Your music, your moments.</Text><Text className="text-white text-xl font-bold px-4 mt-7 mb-4">Recently Added</Text><FlatList horizontal data={songs.slice(0,10)} showsHorizontalScrollIndicator={false} keyExtractor={x=>x._id} renderItem={({item})=><SongCard song={item} queue={songs}/>} contentContainerStyle={{paddingHorizontal:16}}/><Text className="text-white text-xl font-bold px-4 mt-8 mb-2">All Songs</Text>{songs.map(s=><SongRow key={s._id} song={s} queue={songs}/>)}{!songs.length&&<Text className="text-gray-500 text-center py-10 px-4">No songs found. Check the backend connection.</Text>}</ScrollView></SafeAreaView>}